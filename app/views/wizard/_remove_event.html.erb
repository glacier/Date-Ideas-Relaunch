<!-- remove button for an event item in the date cart -->
<%= link_to('Remove', datecart_cart_item_path(datecart, @cart_item), :class => "remove_button button_mute", :remote => true, :method => :delete) %>