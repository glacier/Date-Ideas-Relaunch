<!-- remove button for an event item in the date cart -->
<%= link_to('Remove', datecart_cart_item_path(datecart, @cart_item), :id => TODO_EVENT_ID, :class => "remove_button", :remote => true, :method => :delete) %>