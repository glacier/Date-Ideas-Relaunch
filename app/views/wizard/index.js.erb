//define config object
var dialogOpts = {
	title: "Add Something New",
	modal: true,
	overlay: { 
		background: "url(img/modal.png) repeat" 
	},
	// buttons: {
	// 	"Ok": function() { $(this).dialog("close"); }
	// },
	closeOnEscape: true,
	width: 600,
	autoOpen: false,
	resizable: false,
	draggable: false,
	open: function() {
		//display correct dialog content
		$("#di_add_to_date").html("<%= escape_javascript(render 'form', :format => :html) %>");
	}
};

$("#di_add_to_date").dialog(dialogOpts);
$("#di_add_to_date").dialog("open");